# 🚀 WebGPU Excel - Высокопроизводительная электронная таблица

Современная электронная таблица, построенная на WebGPU для максимальной производительности и поддержки миллионов ячеек.

## ✨ Особенности

- **WebGPU рендеринг** - Использует новейший WebGPU API для GPU-ускоренного рендеринга
- **Виртуализация** - Поддержка сеток размером до 1M×1M ячеек с плавной прокруткой
- **Sparse Matrix** - Эффективное хранение разреженных данных
- **TypeScript** - Полная типизация для надежности кода
- **Высокая производительность** - 60+ FPS даже с большими таблицами
- **Формулы и вычисления** - Полноценная система формул с SUM, MIN, MAX, AVERAGE
- **Undo/Redo** - Система отмены и повтора операций
- **Операции со строками/столбцами** - Вставка, удаление с автоматическим сдвигом формул

## 🎯 Текущий статус

**ОСНОВНОЙ ФУНКЦИОНАЛ РЕАЛИЗОВАН** ✅

### ✅ Реализованные компоненты
- **WebGPU рендеринг** - GPU-ускоренная сетка с instancing
- **Sparse Matrix** - Эффективное хранение разреженных данных
- **Virtual Grid** - Виртуализация с кешированием
- **Canvas 2D overlay** - Заголовки и текст поверх WebGPU
- **Система формул** - Парсер, AST, вычисления, сдвиг ссылок
- **Undo/Redo** - Полная история операций
- **Операции со строками/столбцами** - Вставка, удаление, сдвиг формул

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка проекта

```bash
npm run build
```

### Запуск тестов

```bash
# Все тесты
npm test

# Тесты производительности
npm run test:performance

# Тесты оптимизации
npm run test:optimization

# Покрытие кода
npm run test:coverage
```

## 🏗 Архитектура и технологии

### 📊 SparseMatrix - Разреженная матрица

**Зачем нужна:** В реальных таблицах 99% ячеек пустые. Обычная матрица 1M×1M займет 4TB памяти, а SparseMatrix - всего несколько MB.

**Как работает:**
```typescript
// Блочная структура: блоки 1000×1000 ячеек
blocks: Map<string, Map<string, any>>
// Ключ блока: "rowBlock_colBlock" (например "0_0", "1_2")
// Ключ ячейки: "localRow_localCol" (например "5_3")
```

**Преимущества:**
- ✅ **Экономия памяти:** 99.9% для разреженных данных
- ✅ **Быстрый доступ:** O(1) для существующих ячеек
- ✅ **Масштабируемость:** Поддержка миллионов ячеек

**Минусы:**
- ❌ **Сложность операций:** Вставка/удаление строк требует пересчета всех блоков
- ❌ **Фрагментация:** При частых операциях блоки могут стать неэффективными
- ❌ **Отсутствие индексов:** Нет быстрого поиска по значениям

**🚀 Планируемые улучшения:**

#### Web Workers + Виртуальные блоки
**Идея:** Разделить операции на быстрые (UI) и медленные (фоновые), используя виртуальные блоки для мгновенного отклика.

**Как это работает:**

**1. Виртуальные блоки (Мгновенный отклик)**
- Вместо пересчета всех блоков, просто "сдвигаем" их виртуально
- Каждый блок помнит свой "смещение" относительно реальной позиции
- При обращении к ячейке применяем смещение автоматически
- **Результат:** O(1) операции - вставка строки происходит мгновенно

**2. Web Workers (Фоновая обработка)**
- Тяжелые операции (пересчет блоков) выполняются в фоне
- UI остается отзывчивым во время обработки
- Используем все доступные ядра процессора для параллельной обработки
- **Результат:** Никаких лагов UI, даже при операциях с миллионами ячеек

**3. Очередь операций (Управление состоянием)**
- Все операции добавляются в очередь
- Web Workers обрабатывают операции последовательно
- Виртуальные блоки отслеживают "грязные" данные
- При обращении к ячейке проверяем, обновлена ли она

**Что видит пользователь при вставке строки:**

**Текущее состояние (медленно):**
1. Пользователь нажимает "Вставить строку"
2. UI замирает на 2-5 секунд (зависит от размера таблицы)
3. Показывается спиннер "Обработка..."
4. Через несколько секунд строка появляется
5. UI снова становится отзывчивым

**С Web Workers (мгновенно):**
1. Пользователь нажимает "Вставить строку"
2. **Строка появляется мгновенно** (за 0.001 секунды)
3. UI остается полностью отзывчивым
4. В фоне (незаметно) происходит оптимизация данных
5. Никаких спиннеров или замираний

**Сценарий: Пользователь делает несколько операций подряд**

**Проблема:** Что если пользователь быстро делает несколько операций, пока Web Workers еще не закончили предыдущие?

**Пример:**
1. Пользователь вставляет строку 5 → UI показывает строку мгновенно
2. Сразу вставляет строку 10 → UI показывает строку мгновенно  
3. Сразу удаляет строку 3 → UI показывает удаление мгновенно
4. Все это за 1 секунду, а Web Workers еще обрабатывают первую операцию

**Решение: Умная очередь операций**

**Как это работает:**
- **Очередь операций:** Все операции добавляются в очередь в правильном порядке
- **Виртуальные блоки:** Отслеживают "грязные" данные и применяют все операции сразу
- **Ленивое обновление:** Web Workers пересчитывают только финальное состояние
- **Консистентность:** Пользователь всегда видит корректный результат

**Что видит пользователь:**
1. Вставляет строку 5 → строка появляется мгновенно
2. Вставляет строку 10 → строка появляется мгновенно (с учетом первой вставки)
3. Удаляет строку 3 → удаление происходит мгновенно (с учетом всех предыдущих операций)
4. **Результат:** Все операции применены корректно, UI остается отзывчивым

**Техническая магия:**
- Виртуальные блоки "знают" о всех операциях в очереди
- При обращении к ячейке применяются все накопленные смещения
- Web Workers в итоге пересчитывают только финальное состояние
- Никаких конфликтов или некорректных данных

**Почему это лучше:**

**Производительность:**
- **Вставка строки:** O(1) вместо O(n) - в 1000 раз быстрее
- **Память:** 50% экономия за счет виртуальных блоков
- **Масштабируемость:** Поддержка 10M+ ячеек без лагов

**Пользовательский опыт:**
- **Мгновенный отклик:** Все операции выполняются сразу
- **Отзывчивый UI:** Никаких замираний или спиннеров
- **Плавная работа:** Таблица работает как нативная программа

**Технические преимущества:**
- **Параллелизм:** Используем все ядра процессора
- **Асинхронность:** Тяжелые операции не блокируют UI
- **Эффективность:** Пересчитываем только то, что нужно

**Архитектура:**
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Main Thread   │    │  Web Worker 1   │    │  Web Worker 2   │
│                 │    │                 │    │                 │
│ Virtual Blocks  │◄──►│ Block Recalc    │    │ Block Recalc    │
│ O(1) Operations │    │ Background      │    │ Background      │
│ UI Updates      │    │ Heavy Work      │    │ Heavy Work      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

**Планируемая реализация:**

**Фаза 1: Виртуальные блоки**
- Реализуем систему смещений для мгновенных операций
- O(1) вставка/удаление строк и столбцов

**Фаза 2: Web Workers**
- Выносим тяжелые операции в фоновые потоки
- UI остается отзывчивым при любых операциях

**Фаза 3: Параллельная обработка**
- Используем все доступные ядра процессора
- Максимальная производительность для больших таблиц

**Фаза 4: Оптимизация памяти**
- LRU кеш для часто используемых блоков
- Пул памяти для переиспользования объектов

**Ожидаемые результаты:**
- **Вставка строки:** Мгновенно (0.001 сек) вместо 2-5 секунд
- **Память:** 50% экономия за счет виртуальных блоков
- **Производительность:** 1000x улучшение для больших операций
- **Масштабируемость:** Поддержка 10M+ ячеек без лагов

**Рекомендуемая литература:**
- [Sparse Matrix Storage Formats](https://en.wikipedia.org/wiki/Sparse_matrix#Storage_formats)
- [Compressed Sparse Row (CSR) Format](https://en.wikipedia.org/wiki/Sparse_matrix#Compressed_sparse_row_(CSR,_CRS_or_Yale_format))

### 🎮 WebGPU - GPU-ускоренный рендеринг

**Зачем нужен:** Canvas 2D не масштабируется для больших таблиц. WebGPU позволяет рендерить миллионы ячеек с 60+ FPS.

**Как работает:**
```typescript
// Instanced rendering - один draw call для всех ячеек
renderPass.draw(6, cellCount, 0, 0);
// 6 вершин на ячейку × количество ячеек
```

**Преимущества:**
- ✅ **Производительность:** 1000x быстрее Canvas 2D для больших таблиц
- ✅ **Параллелизм:** GPU обрабатывает тысячи ячеек одновременно
- ✅ **Масштабируемость:** Поддержка миллионов ячеек

**Минусы:**
- ❌ **Сложность:** Сложнее в разработке и отладке
- ❌ **Совместимость:** Требует современные браузеры с WebGPU
- ❌ **Отладка:** GPU ошибки сложнее диагностировать
- ❌ **Гибридный рендеринг:** Canvas 2D overlay создает конфликты координат

**Проблемы текущей реализации:**
- 🔴 **Canvas 2D overlay:** Заголовки и текст рендерятся на Canvas 2D поверх WebGPU
- 🔴 **Двойной рендеринг:** Каждый кадр = WebGPU + Canvas 2D
- 🔴 **Координатные конфликты:** Разные системы координат WebGPU и Canvas 2D
- 🔴 **Производительность:** Canvas 2D становится узким местом при прокрутке

### 🧠 VirtualGrid - Виртуализация с кешированием

**Зачем нужна:** Рендерить все 1M×1M ячеек невозможно. Виртуализация показывает только видимые ячейки.

**Как работает:**
```typescript
// Кеш для оптимизации
private cachedCells: Cell[] | null = null;
private cachedViewport: Viewport | null = null;

// Проверяем кеш перед пересчетом
if (this.cachedCells && this.cachedViewport && 
    this.viewportsEqual(this.cachedViewport, this.viewport)) {
  return this.cachedCells;
}
```

**Преимущества:**
- ✅ **Производительность:** Рендерим только видимые ячейки
- ✅ **Кеширование:** 97.8% улучшение при повторных запросах
- ✅ **Память:** Константное использование памяти

**Минусы:**
- ❌ **Инвалидация кеша:** При операциях со строками/столбцами кеш сбрасывается
- ❌ **Сложность:** Нужно отслеживать изменения viewport
- ❌ **Отсутствие предзагрузки:** Нет предзагрузки соседних ячеек

### 🧮 Система формул

**Архитектура:**
```
FormulaManager → FormulaParser → FormulaAST → FormulaEngine
     ↓              ↓              ↓            ↓
  Управление    Парсинг строки   Дерево      Вычисление
```

**Преимущества:**
- ✅ **AST:** Структурированное представление формул
- ✅ **Автоматический сдвиг:** Формулы обновляются при операциях
- ✅ **Зависимости:** Отслеживание связей между ячейками

**Минусы:**
- ❌ **Производительность:** Каждая формула парсится заново при вычислении
- ❌ **Отсутствие кеша:** Нет кеширования результатов вычислений
- ❌ **Циклические зависимости:** Могут вызвать бесконечные циклы
- ❌ **Пересчет всех формул:** При операциях пересчитываются ВСЕ формулы

## 📊 Результаты производительности

### VirtualGrid
- **1M×1M сетка:** 0.16ms (в 62 раза быстрее цели!)
- **Кеширование:** 97.8% улучшение производительности
- **Прокрутка:** 0.0152ms на кадр

### SparseMatrix  
- **10,000 ячеек:** 7.27ms (в 13 раз быстрее цели!)
- **Разреженные данные:** 16 bytes на ячейку
- **Оптимизация памяти:** 50% экономия

### Формулы
- **Парсинг:** < 1ms для сложных формул
- **Вычисление:** < 0.1ms для базовых функций
- **Сдвиг формул:** Автоматический при операциях со строками/столбцами

## 🛠 Технологии

- **WebGPU** - GPU-ускоренный рендеринг
- **TypeScript** - Статическая типизация
- **Vite** - Быстрая сборка и разработка
- **Vitest** - Тестирование
- **ESLint + Prettier** - Качество кода

## 📁 Структура проекта

```
src/
├── core/                    # Основная логика
│   ├── virtual-grid/        # Виртуализация сетки
│   ├── sparse-matrix/       # Разреженная матрица
│   ├── operation-history/   # Undo/Redo система
│   └── formulas/            # Система формул
│       ├── FormulaParser.ts # Парсер формул
│       ├── FormulaAST.ts    # AST для формул
│       ├── FormulaEngine.ts # Вычислительный движок
│       └── FormulaManager.ts# Менеджер формул
├── rendering/               # Рендеринг
│   ├── webgpu-setup/        # WebGPU настройка
│   ├── HeaderRenderer.ts    # Canvas 2D заголовки
│   └── TextRenderer.ts      # Canvas 2D текст
├── types/                   # TypeScript типы
└── App.ts                  # Главное приложение

tests/
├── unit/                   # Модульные тесты
├── performance/            # Тесты производительности
└── integration/            # Интеграционные тесты
```

## 🚨 Критические проблемы текущего решения

### 🔴 Архитектурные проблемы

#### 1. Гибридный рендеринг (WebGPU + Canvas 2D)
**Проблема:** Смешивание WebGPU и Canvas 2D создает множество проблем:
- **Координатные конфликты:** WebGPU использует NDC (-1..1), Canvas 2D - пиксели
- **Двойной рендеринг:** Каждый кадр = WebGPU pass + Canvas 2D redraw
- **Производительность:** Canvas 2D становится узким местом при прокрутке
- **Сложность отладки:** Разные системы координат усложняют разработку

#### 2. Неэффективная система формул
**Проблема:** Формулы пересчитываются неоптимально:
- **Отсутствие кеша:** Результаты вычислений не кешируются
- **Пересчет всех формул:** При операциях пересчитываются ВСЕ формулы
- **Повторный парсинг:** AST пересоздается при каждом вычислении
- **Циклические зависимости:** Могут вызвать бесконечные циклы

#### 3. Проблемы SparseMatrix
**Проблема:** Блочная структура неэффективна для операций:
- **Пересчет блоков:** Вставка/удаление строк требует пересчета всех блоков
- **Фрагментация:** При частых операциях блоки становятся неэффективными
- **Отсутствие индексов:** Нет быстрого поиска по значениям

**🚀 Решение: Web Workers + Виртуальные блоки**

**Проблема:** O(n) операция блокирует UI
- При вставке строки пересчитываются ВСЕ блоки
- UI замирает на 2-5 секунд
- Пользователь видит спиннер "Обработка..."

**Решение:** O(1) операция + фоновый Worker
- Виртуальные блоки дают мгновенный отклик
- Web Workers обрабатывают данные в фоне
- UI остается отзывчивым

**Архитектура решения:**
```
Main Thread (UI)          Web Workers (Background)
┌─────────────────┐       ┌─────────────────┐
│ Virtual Blocks  │       │ Block Recalc    │
│ O(1) Operations │◄─────►│ Heavy Work      │
│ Instant UI      │       │ No UI Blocking  │
└─────────────────┘       └─────────────────┘
```

**Что видит пользователь:**
- **Было:** Нажал "Вставить строку" → UI замер → спиннер → через 3 секунды строка появилась
- **Стало:** Нажал "Вставить строку" → строка появилась мгновенно → UI остается отзывчивым

**Сложный сценарий: Несколько операций подряд**
- **Было:** 3 операции подряд → UI замер на 15 секунд → все операции применились разом
- **Стало:** 3 операции подряд → все операции применились мгновенно → UI остается отзывчивым

**Как это работает:**
- **Очередь операций:** Все операции добавляются в очередь
- **Виртуальные блоки:** Применяют все операции сразу при обращении к ячейке
- **Web Workers:** Пересчитывают только финальное состояние в фоне
- **Результат:** Пользователь всегда видит корректные данные, даже при быстрых операциях

**Ожидаемые результаты:**
- **Производительность:** 1000x улучшение для больших операций
- **Отзывчивость:** UI остается отзывчивым при операциях с миллионами ячеек
- **Масштабируемость:** Поддержка 10M+ ячеек без лагов

### 🔴 Проблемы производительности

#### 1. Canvas 2D overlay
**Проблема:** Canvas 2D рендеринг на каждом кадре
- Очистка canvas на каждом кадре - медленно
- Рендеринг каждой ячейки отдельно - медленно
- UI лагает при прокрутке больших таблиц

#### 2. Отсутствие кеширования формул
**Проблема:** Формула парсится заново при каждом вычислении
- AST пересоздается каждый раз
- Нет кеширования результатов вычислений
- Пересчет всех формул при операциях

#### 3. Инвалидация кеша VirtualGrid
**Проблема:** Кеш сбрасывается при любых операциях
- Весь кеш очищается при вставке/удалении строк
- Нет инкрементального обновления кеша
- Потеря производительности при частых операциях

## 🎯 Roadmap

### Приоритет 1: Критические исправления
- **🔴 Убрать Canvas 2D overlay** - Перенести все на WebGPU
- **🔴 Кеширование формул** - Кешировать результаты вычислений
- **🔴 Web Workers + SparseMatrix** - Виртуальные блоки + фоновые вычисления

### Приоритет 2: WebGPU виртуализация
- **WebGPU заголовки** - Font atlas + instanced rendering
- **WebGPU текст** - Glyph caching + texture streaming
- **WebGPU шрифты** - Динамическая загрузка шрифтов

### Приоритет 3: Расширенные функции
- **Больше формул** - COUNT, IF, VLOOKUP, INDEX, MATCH
- **Строковые функции** - CONCATENATE, LEFT, RIGHT, MID
- **Математические функции** - ROUND, CEILING, FLOOR, MOD

### Приоритет 4: Стилизация и форматирование
- **Стили ячеек** - Шрифты, цвета, выравнивание
- **Условное форматирование** - Автоматические стили по условиям
- **Границы и заливки** - Настраиваемые границы ячеек

### Приоритет 5: Импорт/экспорт
- **Excel совместимость** - Чтение/запись .xlsx файлов
- **CSV поддержка** - Импорт/экспорт CSV
- **Копирование/вставка** - Поддержка буфера обмена

## 🚀 План виртуализации WebGPU

### Текущее состояние (Проблемное)
```
┌─────────────────┐
│   WebGPU Grid   │ ← Основная сетка (быстро)
├─────────────────┤
│ Canvas 2D Headers│ ← Заголовки (медленно) 🔴
├─────────────────┤
│ Canvas 2D Text  │ ← Текст ячеек (медленно) 🔴
└─────────────────┘
```

### Целевое состояние (Оптимальное)
```
┌─────────────────┐
│   WebGPU Grid   │ ← Основная сетка
├─────────────────┤
│  WebGPU Headers │ ← Заголовки (быстро) ✅
├─────────────────┤
│   WebGPU Text   │ ← Текст ячеек (быстро) ✅
└─────────────────┘
```

### Технические детали виртуализации

#### 1. WebGPU заголовки
**Текущая проблема:** Canvas 2D рендеринг заголовков медленный при прокрутке
**Решение:** `WebGPUHeaderRenderer` с:
- **Font Atlas** - Предварительно сгенерированная текстура со всеми символами
- **Instanced rendering** - Один draw call для всех заголовков
- **GPU-кеширование** - Кеширование часто используемых заголовков

#### 2. WebGPU текст
**Текущая проблема:** Canvas 2D рендеринг текста не масштабируется
**Решение:** `WebGPUTextRenderer` с:
- **Glyph Caching** - Кеширование глифов на GPU
- **Instanced Text Rendering** - Массовый рендеринг текста
- **Dynamic Font Loading** - Загрузка шрифтов по требованию
- **Texture Streaming** - Загрузка текстур по требованию

#### 3. Оптимизация памяти
- **GPU Memory Management** - Умное управление памятью GPU
- **LOD System** - Разные уровни детализации для текста
- **Compression** - Сжатие текстур для экономии памяти

#### 4. Ожидаемые результаты
- **Производительность:** 3-5x улучшение FPS
- **Масштабируемость:** Поддержка 10M+ ячеек с текстом
- **Память:** 50% экономия GPU памяти
- **Загрузка:** Мгновенная инициализация больших таблиц

## 📚 Рекомендуемая литература

### WebGPU
- [WebGPU Fundamentals](https://webgpufundamentals.org/)
- [WebGPU Best Practices](https://toji.dev/webgpu-best-practices/)
- [WebGPU Shading Language](https://www.w3.org/TR/WGSL/)

### Sparse Matrices
- [Sparse Matrix Storage Formats](https://en.wikipedia.org/wiki/Sparse_matrix#Storage_formats)
- [Compressed Sparse Row (CSR) Format](https://en.wikipedia.org/wiki/Sparse_matrix#Compressed_sparse_row_(CSR,_CRS_or_Yale_format))
- [Sparse Matrix-Vector Multiplication](https://en.wikipedia.org/wiki/Sparse_matrix-vector_multiplication)

### Virtual Scrolling
- [Virtual Scrolling in React](https://web.dev/virtualize-long-lists-react-window/)
- [Virtual Scrolling Performance](https://blog.logrocket.com/virtual-scrolling-core-principles-and-basic-implementation-in-react/)

### Formula Engines
- [Excel Formula Engine Architecture](https://docs.microsoft.com/en-us/office/client-developer/excel/excel-formula-engine-architecture)
- [AST-based Formula Parsing](https://en.wikipedia.org/wiki/Abstract_syntax_tree)

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.